module.exports = {
    resolve: {
      alias: {
        '@': path.resolve(__dirname, 'src'), // Assuming your components are in a 'src' directory
      },
    },
    // Other Webpack configuration...
  };
  